---
title: "DRMQ: Distributed, Reliable Message Queue for Java"
sidebarTitle: "Introduction"
description: "DRMQ is a fault-tolerant distributed message broker built from first principles, with guaranteed message ordering and high availability via Raft consensus."
---

DRMQ is a high-performance, fault-tolerant distributed message broker built from first principles. It uses a full implementation of the Raft consensus algorithm for leader election and log replication, giving you strong ordering guarantees and high availability without relying on external coordination services. Whether you're running a single node for local development or a three-node production cluster with automatic failover, DRMQ exposes the same clean producer and consumer API throughout.

## Key Features

<CardGroup cols={2}>
  <Card title="Consumer Groups" icon="users" href="/concepts/consumer-groups">
    Scale out message processing across multiple consumers without partitions. The broker automatically load-balances delivery within a group and redelivers uncommitted messages if a consumer fails.
  </Card>
  <Card title="Dead-Letter Queues" icon="circle-xmark" href="/features/dead-letter-queues">
    Isolate poison-pill messages automatically. After a configurable number of delivery failures, DRMQ routes the offending message to a dedicated DLQ topic so the rest of your queue keeps flowing.
  </Card>
  <Card title="Cluster Mode" icon="server" href="/concepts/cluster-mode">
    Run a multi-node cluster with full Raft replication, quorum-loss stepdown, and follower-based reads for horizontal read scalability.
  </Card>
  <Card title="Monitoring" icon="chart-line" href="/features/monitoring">
    Integrated Micrometer and Prometheus metrics expose broker health, replication lag, and throughput—ready to drop into any Grafana dashboard.
  </Card>
</CardGroup>

## Deployment Modes

DRMQ ships with two deployment modes so you can match the setup to your environment:

- **Single-node mode** — start one broker process, zero configuration beyond a port and data directory. Ideal for local development, CI pipelines, and simple deployments that don't require fault tolerance.
- **Cluster mode** — start three or more broker nodes, each aware of its peers. Raft elects a leader, replicates every write to a quorum, and automatically steps down an isolated leader to prevent split-brain data loss. Clients connect to any node and are transparently redirected to the current leader.

## Prerequisites

Before you build or run DRMQ, make sure your environment meets these requirements:

| Requirement | Minimum version |
|---|---|
| Java (JDK) | 17 |
| Apache Maven | 3.8.x |

<Note>
  The repository includes a Maven wrapper (`mvnw` / `mvnw.cmd`) so you don't need a system-wide Maven installation. See the [Installation guide](/installation) for details.
</Note>

## How It Works

DRMQ uses a custom TCP protocol backed by Protocol Buffers for all client-broker and broker-to-broker communication. The broker persists every message to a Write-Ahead Log before acknowledging a write, so no data is lost even if a node crashes immediately after a successful send.

In cluster mode, the Raft consensus algorithm replicates each WAL entry to a quorum of nodes before the leader acknowledges the write to the client. If the leader fails, the remaining nodes elect a new leader and resume serving traffic automatically. The Java client detects `NOT_LEADER` responses and transparently reconnects to the current leader, so your application code never needs to handle cluster topology changes directly.

## Next Steps

Ready to send your first message? Follow the [Quickstart](/quickstart) to have a broker running and a message round-tripped in under five minutes.
